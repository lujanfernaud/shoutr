<%= form_for :search, url: search_path, method: :get do |form| %>
  <%= form.label :term, hidden: true %>
  <%= form.text_field :term, placeholder: "Search here..." %>
  <%= form.submit "Search" %>
<% end %>
